name: fb_audience_network_ad_example
description: Demonstrates how to use the fb_audience_network_ad plugin.
publish_to: 'none'

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  fb_audience_network_ad:
    path: ../

flutter:

  uses-material-design: true
